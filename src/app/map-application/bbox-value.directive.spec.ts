import { BboxValueDirective } from './bbox-value.directive';

describe('BboxValueDirective', () => {
  it('should create an instance', () => {
    const directive = new BboxValueDirective();
    expect(directive).toBeTruthy();
  });
});
